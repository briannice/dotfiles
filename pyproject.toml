[project]
name = "dotfiles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "briannice", email = "brian.nys.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
dotfiles = "dotfiles:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
